{
  "name": "advnoemia-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --workspace portal-backend",
    "build:portal": "npm run build --workspace portal-backend",
    "lint": "npm run lint --workspace portal-backend",
    "operations:verify": "npm run operations:verify --workspace portal-backend",
    "operations:verify:ci": "npm run operations:verify:ci --workspace portal-backend",
    "operations:verify:release": "npm run operations:verify:release --workspace portal-backend",
    "operations:evidence:ci": "npm run operations:evidence:ci --workspace portal-backend",
    "operations:evidence:release": "npm run operations:evidence:release --workspace portal-backend",
    "smoke:phase10": "npm run smoke:phase10 --workspace portal-backend",
    "typecheck": "npm run typecheck --workspace portal-backend",
    "test": "npm run test --workspace portal-backend",
    "start": "npm run start --workspace portal-backend",
    "dev": "npm run dev --workspace portal-backend"
  },
  "workspaces": [
    "apps/portal-backend"
  ]
}
